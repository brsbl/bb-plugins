# Shared packages

No runtime helper has met the extraction threshold yet. Plugin-owned SDK declarations and vendored UI primitives stay with each plugin; stable repository-wide behavior lives in `tooling/`.
