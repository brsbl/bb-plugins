.DS_Store
node_modules/
coverage/
.otto/
*.tgz
!tooling/vendor/*.tgz

# Production bundles are created and validated in CI.
dist/
plugins/.scaffold-smoke-*

# Plugin build toolchain scratch dirs (resolved on demand, never committed).
toolchain-*/
