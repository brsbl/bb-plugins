// The source-native v3 compatibility facade is intentionally separate from the
// retained gallery catalog and preview adapters.
export {
  cliCommandInfo,
  cliEnvelopeVersion,
  cliSchemaVersion,
  runAtlasCli,
} from "./atlas-cli-v3.js";
