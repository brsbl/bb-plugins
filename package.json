{
  "name": "bb-plugin-theme-preview",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "files": [
    "assets",
    "dist",
    "docs",
    "skills",
    "README.md"
  ],
  "engines": {
    "bb": ">=0.38.0",
    "bbPluginSdk": ">=0.4.6"
  },
  "bb": {
    "name": "Theme Preview",
    "description": "A skeleton of the bb app in every configuration — sidebar, splits, panels, overlays, threads — rendered from the active theme's tokens so a palette can be judged before it ships.",
    "branding": {
      "icon": "./assets/swatches.svg"
    },
    "server": "./dist/install-server.mjs",
    "app": "./dist/install-app.mjs",
    "skills": [
      "skills"
    ]
  }
}
