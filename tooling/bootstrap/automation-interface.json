{
  "schemaVersion": 1,
  "note": "Verified bb automation interface used to keep generated bootstrap prompts on current commands and permission modes. Update this only after re-checking `bb plugin run automations create --help` and the automations plugin source. Validation is text-only and never creates or mutates a live automation.",
  "verifiedAgainst": {
    "installedCli": "bb-app 0.0.32 (permissionModeSchema = enum[full, workspace-write, readonly])",
    "hostSource": "automations plugin rpc-types.ts (permissionModeSchema = enum[accept-edits, auto, full]); data.ts migrates workspace-write/readonly -> accept-edits",
    "helpCommand": "bb plugin run automations create --help"
  },
  "command": {
    "canonicalPrefix": "bb plugin run automations",
    "createForm": "bb plugin run automations create",
    "deprecatedForms": ["bb automations create", "bb automation add", "bb create-automation"]
  },
  "permissionModes": {
    "recommended": "full",
    "current": ["accept-edits", "auto", "full"],
    "legacy": ["workspace-write", "readonly"],
    "rationale": "full is valid on both the installed release and current host builds and is what an unattended edit-and-commit maintenance run needs. workspace-write/readonly are legacy and being folded into accept-edits, so a generated prompt must not hard-code them."
  }
}
